# frontend/.env.production
# Used by Vite during `npm run build`
# Set VITE_API_BASE_URL in Netlify Dashboard → Site Settings → Environment Variables
# Leave empty to use the /api proxy defined in netlify.toml
VITE_API_BASE_URL=
VITE_APP_NAME=Rupeezy AI Voice RM
